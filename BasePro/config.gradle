def releaseTime() {
    return new Date().format("yyyyMMdd", TimeZone.getTimeZone("GMT+08:00"))
}

ext {

    buildToolsVersion = "28.0.3"
    versionName = "${releaseTime()}"
    minSdkVersion = 14
    sdkVersion = 28
    lib_ver = sdkVersion + '.0.0'


    versionCode = 1
    isRelease = false

//    if (isRelease) {
        applicationId = "com.studyhelper.baselib"
//    } else {
//        applicationId = "com.studyhelper.maindebug"
//    }

}